# HTML/CSS/JS 專案專屬工作守則

## 1. 專案技術規範
- 本專案為純前端專案，使用原生 HTML5, CSS3, 與 Vanilla JavaScript (ES6+)。
- 除非外在明確要求，否則禁止引進任何 npm 外部套件或建構工具（如 Webpack, Vite, Tailwind CLI 等）。
- 樣式調整優先使用專案現有的 CSS 檔案，避免在 HTML 中寫大量的 inline style。

## 2. 工具使用限制
- 禁止執行 `npm install`、`yarn` 等套件管理指令。
- 尋找程式碼時，請精準使用 `read_file` 讀取特定 HTML/CSS/JS，避免使用全域 `search_grep` 掃描整個資料夾。
- 在修改程式碼前，必須先向使用者說明修改邏輯，取得同意後再動手。

## 3. 程式碼品質與相容性
- 所有的 JavaScript 必須維持原生相容性，並做好 DOM 元素是否存在的防呆檢查（例如 `if (element) ...`）。
- 保持 HTML 語意化（Semantic HTML），並確保 CSS 具備良好的響應式設計（RWD）。

## 4. TPM (Token Per Minute) 節約與高效率協作守則
- **精準局部讀取**：對大型檔案（如 `index.html`、`js/` 腳本等），讀取時優先指定 `start_line` 與 `end_line` 行數範圍，避免整檔載入 Context。
- **嚴禁讀取暫存檔**：除非使用者明確指定，否則禁止讀取 `temp.css` 或任何備份/暫存檔。
- **精簡回應與極速修改**：回答直奔重點，修改程式碼時僅提供受影響的程式碼片段或透過 Editor 工具局部替換，避免重複重印無關代碼。
- **並行工具呼叫**：若需要讀取多檔或執行多指令，必須在單一次回應中並行調用，減少無效對話回合數。
- **任務結束提示**：當一個獨立需求或 Bug 完成時，提醒使用者開啟 New Task，以釋放長對話積累的 Token 負載。
